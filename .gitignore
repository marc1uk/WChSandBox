# don't track text editor temp files
*~
# or swap disk temp files
*.swp
# some kind of Mac files we don't want to track
.DS_Store
#compiler files - don't need to be stored, they're generated by the compiler
*.o
*.so
*.a
*.dylib
*.pyc
# we can invert some files so that they are stored:
#!lib.a	# DO track lib.a, even though we've said don't track *.a files

#ignore build results
bin/
fluxesandtables/
output_files/
tmp/
unused/
*.root

quick
summary_and_todo
